<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pastelaria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pastelaria">
        <activity android:name=".view.CadastroPedidoActivity"></activity>
        <activity android:name=".view.ListarPedidosActivity" />
        <activity android:name=".view.CadastroBebidaActivity" />
        <activity android:name=".view.ListarBebidasActivity" />
        <activity android:name=".view.CadastroPastelActivity" />
        <activity android:name=".view.ListarPastelActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>